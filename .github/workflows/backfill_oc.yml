name: Backfill OC (Datos Abiertos)

on:
  workflow_dispatch:
    inputs:
      url:
        description: "Un mes: URL completa o solo AÑO-MES (ej: 2026-6). Vacío = usar rango."
        required: false
        default: ""
      desde:
        description: "Rango (opcional): mes inicial AÑO-MES, ej: 2025-7"
        required: false
        default: ""
      hasta:
        description: "Rango (opcional): mes final AÑO-MES, ej: 2026-6"
        required: false
        default: ""
      dry_run:
        description: "Prueba: solo muestra el mapeo de columnas y cuenta, sin guardar ni publicar"
        type: boolean
        required: false
        default: true

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  backfill-oc:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Instalar dependencias
        run: pip install -r requirements.txt

      - name: Importar OC de Datos Abiertos
        env:
          PYTHONUNBUFFERED: "1"
        run: |
          DRY=""
          if [ "${{ inputs.dry_run }}" = "true" ]; then DRY="--dry-run"; fi
          if [ -n "${{ inputs.desde }}" ] && [ -n "${{ inputs.hasta }}" ]; then
            python pipeline.py import-da --desde "${{ inputs.desde }}" --hasta "${{ inputs.hasta }}" $DRY
          elif [ -n "${{ inputs.url }}" ]; then
            python pipeline.py import-da --url "${{ inputs.url }}" $DRY
          else
            echo "Debes indicar 'url' (un mes) o 'desde'+'hasta' (rango)."; exit 1
          fi

      - name: Reconstruir el panel
        if: ${{ inputs.dry_run == false }}
        run: python pipeline.py build

      - name: Guardar la base en el repositorio
        if: ${{ inputs.dry_run == false }}
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add mp_traslados.db dashboard.html
          git commit -m "Backfill OC (Datos Abiertos)" || echo "Sin cambios"
          git push

      - name: Configurar Pages
        if: ${{ inputs.dry_run == false }}
        uses: actions/configure-pages@v5

      - name: Subir artefacto de Pages
        if: ${{ inputs.dry_run == false }}
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - name: Publicar en Pages
        if: ${{ inputs.dry_run == false }}
        uses: actions/deploy-pages@v4
